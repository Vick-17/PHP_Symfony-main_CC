{% extends 'base.html.twig' %}

{% block title %}Modifier un hôtel{% endblock %}

{% block body %}
<h1 class="mb-4">Modifier l'hôtel {{ hotel.nom }}</h1>

<form method="post" action="{{ path('hotel_edit', { id: hotel.id }) }}">
    <div class="mb-3">
        <label for="nom" class="form-label">Nom de l'hôtel</label>
        <input type="text" id="nom" name="nom" class="form-control" value="{{ hotel.nom }}" required>
    </div>
    <div class="mb-3">
        <label for="adresse" class="form-label">Adresse</label>
        <input type="text" id="adresse" name="adresse" class="form-control" value="{{ hotel.adresse }}" required>
    </div>
    <div class="mb-3">
        <label for="ville" class="form-label">Ville</label>
        <input type="text" id="ville" name="ville" class="form-control" value="{{ hotel.ville }}" required>
    </div>
    <div class="mb-3">
        <label for="telephone" class="form-label">Téléphone</label>
        <input type="text" id="telephone" name="telephone" class="form-control" value="{{ hotel.telephone }}" required>
    </div>
    <div class="mb-3">
        <label for="categorie" class="form-label">Catégorie</label>
        <select id="categorie" name="categorie" class="form-control" required>
            <option value="1" {% if hotel.categorie == 1 %}selected{% endif %}>1 étoile</option>
            <option value="2" {% if hotel.categorie == 2 %}selected{% endif %}>2 étoiles</option>
            <option value="3" {% if hotel.categorie == 3 %}selected{% endif %}>3 étoiles</option>
            <option value="4" {% if hotel.categorie == 4 %}selected{% endif %}>4 étoiles</option>
            <option value="5" {% if hotel.categorie == 5 %}selected{% endif %}>5 étoiles</option>
        </select>
    </div>
    <button type="submit" class="btn btn-success">Enregistrer</button>
    <a href="{{ path('home') }}" class="btn btn-secondary">Annuler</a>
</form>
{% endblock %}